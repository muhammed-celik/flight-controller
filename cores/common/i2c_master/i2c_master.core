CAPI=2:
name: ::i2c_master:1.0.0
description: SPI master

filesets:
  rtl:
    files:
      - src/i2c_master_pkg.sv
      - src/i2c_clk_gen.sv
      - src/i2c_master.sv
    file_type: systemVerilogSource

  sim:
    files:
      - tb/i2c_master_tb.sv: {file_type: systemVerilogSource}
      - tb/cocotb_env.cpp: {file_type: cppSource, copyto: cocotb_env.cpp}
      - tb/test_i2c_master.py: {file_type: user, copyto: test_i2c_master.py}

targets:
  default:
    filesets: [rtl]
    toplevel: i2c_master

  sim:
    filesets: [rtl, sim]
    toplevel: i2c_master_tb
    flow: sim
    flow_options:
      tool: verilator
      cocotb_module: test_i2c_master
      mode: cc
      verilator_options:
        - --trace
        - -Wno-WIDTHEXPAND
        - -LDFLAGS "-Wl,--disable-new-dtags,-rpath,$$(dirname $$(cocotb-config --libpython))"
      run_options: [--trace, --trace-file, i2c_master.vcd]
